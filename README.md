# 小钱哥短剧工厂

Windows 10/11 客户版，99 元 / 31 天。客户授权后绑定一台电脑，生成费用包含在客户自己的专属套餐中，不与其他客户共用。

## 使用

1. 安装 Node.js 22 LTS。
2. 解压本 ZIP，不要移动或删除 `backend`、`frontend`、`tools` 文件夹。
3. 双击 `start.bat`，浏览器打开本地工作台。
4. 输入购买后收到的一次性产品授权码。
5. 停止服务时双击 `stop.bat`。

## 版本更新

程序启动后会自动检查官方更新。发现新版本时，工作台会显示更新提示；点击更新后会先校验更新包并备份本地数据库，再替换程序文件并自动重启。更新不会覆盖 `%APPDATA%\小钱哥短剧工厂` 中的数据库、授权信息、备份，也不会覆盖 `%USERPROFILE%\Videos\小钱哥短剧工厂` 中的作品。

本程序不提供 EXE 安装器。数据库、授权信息和备份保存在 `%APPDATA%\小钱哥短剧工厂`，视频和导出文件保存在 `%USERPROFILE%\Videos\小钱哥短剧工厂`。

## 购买与支持

QQ：3184958887

官网：[www.hhtc.top](https://www.hhtc.top)

授权码由销售方人工开通。到期后不能创建新的生成任务，但已有项目仍可打开、备份和导出。

## 说明

本软件只把生成请求发送到小钱哥短剧工厂的云端服务，客户不需要填写模型地址或 API 密钥。请阅读 `docs` 目录中的隐私政策、订阅条款、AI 内容责任和第三方服务说明。
